set default="1"

function load_video {
  insmod efi_gop
  insmod efi_uga
  insmod video_bochs
  insmod video_cirrus
  insmod all_video
}

load_video
set gfxpayload=keep
insmod gzio
insmod part_gpt
insmod ext2

set timeout=60
### END /etc/grub.d/00_header ###

search --no-floppy --set=root -l 'KICKSTART'

menuentry 'Install Fedora 22' --class fedora --class gnu-linux --class gnu --class os {
	linuxefi /vmlinuz inst.stage2=hd:LABEL=KICKSTART:/squashfs.img ks=hd:LABEL=KICKSTART:/ks.cfg raid=noautodetect
	initrdefi /initrd.img
}
